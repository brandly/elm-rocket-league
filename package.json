{
  "scripts": {
    "start": "elm reactor",
    "build": "elm make src/Main.elm",
    "test": "elm-analyse && npm run build -- --optimize",
    "deploy": "npm run build -- --optimize && mkdir -p dist/static && mv index.html dist && cp src/static/* dist/static && gh-pages --dist dist/"
  },
  "devDependencies": {
    "elm": "0.19.1-3",
    "elm-analyse": "0.16.5",
    "gh-pages": "3.1.0"
  }
}
